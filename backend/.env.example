# configurações / segredos
PORT=
FRONTEND_URL=
NODE_ENV=
# Admin SDK: ficheiro `config/credential.json` (chave da conta de serviço Firebase; não commitar).
# Em produção o GitHub Actions gera este ficheiro a partir dos secrets (ver aws-backend-ecr.yml).
# Login (Identity Toolkit): chave Web do MESMO projeto Firebase — Project settings → General → Web API Key.
# Não está dentro do JSON da conta de serviço; tem de estar em variável de ambiente.
FIREBASE_WEB_API_KEY=
